{
  "name": "appget.docs",
  "scripts": {
    "serve": "vuepress dev docs",
    "test": "mdspell **/*.md !**/node_modules/**/*.md -r -n -a",
    "build": "vuepress build docs",
    "surge": "surge --project ./dist --domain docs.appget.net"
  },
  "dependencies": {
    "markdown-spellcheck": "^1.3.1",
    "vuepress": "^0.10.0"
  },
  "devDependencies": {
    "surge": "^0.20.1"
  }
}
