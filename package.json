{
  "name": "appget.docs",
  "scripts": {
    "serve": "vuepress dev docs",
    "build": "vuepress build docs",
    "surge": "surge --project ./dist --domain docs.appget.net"
  },
  "dependencies": {
    "vuepress": "^0.8.4"
  },
  "devDependencies": {
    "surge": "^0.20.1"
  }
}
